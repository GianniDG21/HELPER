[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: richiede Docker Postgres (imposta HELPER_TEST_USE_REAL_DB=1)
